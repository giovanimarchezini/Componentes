import React from 'react'
import './Input.css';

const InputRoot = ({children}) => {
  return (
    <div className="input-field">
      {children}
    </div>
  )
}

export default InputRoot;
